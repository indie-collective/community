import React from 'react';

const People = () => {
  // const games = useQuery({
  //   query: detailedGames,
  // })[0];

  return (
    <div>
      People
    </div>
  );
};

export default People;
