import React from 'react';

const Entities = () => {
  // const games = useQuery({
  //   query: detailedGames,
  // })[0];

  return (
    <div>
      Entities
    </div>
  );
};

export default Entities;
