import React from 'react';

const Events = () => {
  // const games = useQuery({
  //   query: detailedGames,
  // })[0];

  return (
    <div>
      Events
    </div>
  );
};

export default Events;
