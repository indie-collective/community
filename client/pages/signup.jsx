import React from 'react';

import SignupForm from '/components/SignupForm';

const Signup = ({ id }) => {
  // query

  return (
    <div>
      <SignupForm />
    </div>
  );
};

export default Signup;
